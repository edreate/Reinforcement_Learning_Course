[project]
name = "reinforcement-learning-course"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gymnasium",
    "install",
    "matplotlib",
    "numpy",
    "ruff",
    "torch",
]
